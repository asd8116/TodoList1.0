<a href="/todos/new">Create</a>

<ul>
  {{#each todos}}
  <li>
    {{{this.name}}}
    <a href="/todos/{{{this.id}}}">detail</a>
    <a href="/todos/{{{this.id}}}/edit">edit</a>
    <form action="/todos/{{{this.id}}}/delete" method="POST" style="display: inline;">
      <button type="submit">delete</button>
    </form>
  </li>
  {{/each}}
</ul>